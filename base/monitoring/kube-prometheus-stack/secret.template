apiVersion: v1
kind: Secret
metadata:
    name: kube-prometheus-stack-helm-values
    namespace: monitoring
type: Opaque
stringData:
    values.yaml: |
        alertmanager:
          config:
            global:
              slack_api_url:
        grafana:
          grafana.ini:
            auth.generic_oauth:
              client_id:
              client_secret:
