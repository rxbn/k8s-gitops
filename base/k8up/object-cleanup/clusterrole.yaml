---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: object-cleanup
rules:
  - apiGroups:
      - backup.appuio.ch
    resources:
      - backups
      - checks
      - prunes
    verbs:
      - get
      - list
      - delete
