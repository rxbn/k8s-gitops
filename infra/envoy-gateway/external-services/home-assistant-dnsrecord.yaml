---
apiVersion: cloudflare-operator.io/v1
kind: DNSRecord
metadata:
  name: home-${BASE_DOMAIN/./-}
  namespace: cloudflare-operator
spec:
  name: home.${BASE_DOMAIN}
  type: CNAME
  content: ${BASE_DOMAIN}
  proxied: true
  ttl: 1
