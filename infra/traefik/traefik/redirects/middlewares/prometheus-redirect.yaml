---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: prometheus-redirect
  namespace: traefik
spec:
  redirectRegex:
    regex: ^https://${BASE_DOMAIN}/pr$
    replacement: https://prometheus.local.${BASE_DOMAIN}
