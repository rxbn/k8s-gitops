---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: teslamate-grafana-redirect
  namespace: traefik
spec:
  redirectRegex:
    regex: ^https://${BASE_DOMAIN}/tmg$
    replacement: https://teslamate-grafana.${BASE_DOMAIN}
