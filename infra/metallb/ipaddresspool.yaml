---
apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: default
  namespace: metallb-system
spec:
  addresses:
    - ${METALLB_ADDRESS_POOL_V4}
    - ${METALLB_ADDRESS_POOL_V6}
  autoAssign: true
