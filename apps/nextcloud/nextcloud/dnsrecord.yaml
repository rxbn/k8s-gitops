---
apiVersion: cloudflare-operator.io/v1
kind: DNSRecord
metadata:
  name: cloud-${BASE_DOMAIN/./-}
  namespace: cloudflare-operator
spec:
  name: cloud.${BASE_DOMAIN}
  type: CNAME
  content: ${BASE_DOMAIN}
  proxied: true
  ttl: 1
