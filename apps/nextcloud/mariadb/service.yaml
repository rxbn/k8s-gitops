---
apiVersion: v1
kind: Service
metadata:
  name: nextcloud-mariadb
  namespace: nextcloud
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: nextcloud-mariadb
  type: ClusterIP
