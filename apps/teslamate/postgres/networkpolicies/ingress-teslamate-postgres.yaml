---
apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: ingress-teslamate-postgres
  namespace: teslamate
spec:
  selector: app == 'teslamate-postgres'
  types:
    - Ingress
  ingress:
    - action: Allow
      protocol: TCP
      source:
        selector: app == 'teslamate'
      destination:
        ports:
          - 5432
