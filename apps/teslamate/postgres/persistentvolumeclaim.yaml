---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teslamate-postgres-data
  namespace: teslamate
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nvme
  resources:
    requests:
      storage: 100Mi
